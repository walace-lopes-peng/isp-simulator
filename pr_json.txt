{"baseRefName":"dev","body":"## Summary\n- Resolved unresolved merge conflict markers in `.github/workflows/board-sync.yml`\n- Consolidated push logic to use a single `BRANCH_NAME` variable instead of repeated inline expressions\n- Kept pre-push `git pull --rebase` to guard against concurrent pushes\n\n## Test plan\n- [ ] YAML is valid (no conflict markers)\n- [ ] Workflow triggers correctly on push to `dev`, issue events, and PR events\n- [ ] `$BRANCH_NAME` resolves correctly for both PR and push contexts\n\nCloses #150","headRefName":"feature/board-sync-fix","mergeable":"CONFLICTING","state":"OPEN","title":"fix: resolve merge conflict in board-sync.yml (closes #150)"}
